export default function cloudConditionIcons(condition) {

    if (condition === "Rainy") return "🌧️"

    if (condition === "Sunny") return "🌤️"

    if (condition === "Partly cloudy") return "⛅"

    if (condition === "Mostly cloudy") return "☁️"

    if (condition === "Overcast") return "☁️"
}